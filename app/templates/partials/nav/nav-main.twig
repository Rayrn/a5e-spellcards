{% set headerItems = [
    { name: 'Home', url: path('home') },
    { name: 'Cast Random Spells', url: path('spell--random') },
    { name: 'Open Spellbook', url: path('spell--book') }
] %}

<nav class="navbar navbar-expand-lg navbar-dark bg-dark position-sticky top-0 start-0 end-0 z-3">
    <div class="container">{% for navItem in headerItems %}{% include 'partials/nav/nav-item.twig' with { navItem: navItem } only %}{% endfor %}</div>
</nav>
