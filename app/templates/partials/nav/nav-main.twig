{% set headerItems = [
    { name: 'Home', url: path('home') },
    { name: 'Random Spell Card', url: path('spell--random') },
    { name: 'Spells By...', url: '#', dropdown: [
        { name: 'Class ', url: path('spell--class') },
        { name: 'School', url: path('spell--school') },
        { name: 'Level', url: path('spell--level') }
    ]}
] %}

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">{% for navItem in headerItems %}{% include 'partials/nav/nav-item.twig' with { navItem: navItem } only %}{% endfor %}</div>
</nav>
