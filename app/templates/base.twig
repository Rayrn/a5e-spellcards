<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{%- block title -%} {{ app_display_name }} | {{ app_version }}{%- endblock -%}</title>
        <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon">
        <link rel="icon" href="/favicon.ico" type="image/x-icon">

        {% block stylesheets %}
            {{- encore_entry_link_tags('app') -}}
        {%- endblock %}

        {% block javascripts %}
            {{- encore_entry_script_tags('app') -}}
        {%- endblock %}
    </head>
    <body class="bg-dark-subtle">
        {% include 'partials/nav/nav-main.twig'%}

        <div class="row text-center justify-content-center">
            <div class="col-sm-10 col-xl-8 bg-light">
                {% include 'partials/elements/title.twig' with { 'title': app_display_name } %}

                {% block body %}{% endblock %}
            </div>
        </div>

        {% include 'partials/nav/nav-footer.twig' %}

        {% block modals %}{% endblock %}
    </body>
</html>
